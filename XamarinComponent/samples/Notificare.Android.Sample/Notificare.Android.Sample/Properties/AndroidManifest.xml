<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" android:versionCode="10300" android:versionName="1.3.0" package="notificare.android.sample">
	<uses-sdk android:minSdkVersion="10" android:targetSdkVersion="21" />
	<!-- PERMISSIONS -->
	<uses-permission android:name="android.permission.GET_ACCOUNTS" />
	<uses-permission android:name="android.permission.WAKE_LOCK" />
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
	<uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
	<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
	<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
	<!-- This app has permission to register and receive message -->
	<uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />
	<!-- 
		PERMISSIONS REQUIRED for Google Cloud Messaging  
		Please replace "com.example.pushtest" with your own package name
	-->
	<permission android:name="notificare.android.sample.permission.C2D_MESSAGE" android:protectionLevel="signature" />
	<uses-permission android:name="notificare.android.sample.permission.C2D_MESSAGE" />
	<uses-permission android:name="android.permission.BLUETOOTH" />
	<uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
	<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
	<application android:label="Notificare.Android.Sample" android:icon="@drawable/icon">
		<!--
	      BroadcastReceiver that will receive intents from GCM
	      services and handle them to your app's IntentService.
	 
	      The com.google.android.c2dm.permission.SEND permission is necessary
	      so only GCM services can send data messages for the app.
	    -->
		<receiver android:name="re.notifica.push.gcm.PushReceiver" android:permission="com.google.android.c2dm.permission.SEND">
			<intent-filter>
				<!-- Receives the actual messages. -->
				<action android:name="com.google.android.c2dm.intent.RECEIVE" />
				<!-- Receives the registration id. -->
				<action android:name="com.google.android.c2dm.intent.REGISTRATION" />
				<!-- Replace with your package name -->
				<category android:name="notificare.android.sample" />
			</intent-filter>
		</receiver>
		<receiver android:name="org.altbeacon.beacon.startup.StartupBroadcastReceiver">
			<intent-filter>
				<action android:name="android.intent.action.BOOT_COMPLETED" />
				<action android:name="android.intent.action.ACTION_POWER_CONNECTED" />
				<action android:name="android.intent.action.ACTION_POWER_DISCONNECTED" />
			</intent-filter>
		</receiver>
		<service android:name="org.altbeacon.beacon.service.BeaconService" android:enabled="true" android:exported="true" android:isolatedProcess="false" android:label="beacon">
		</service>
		<service android:name="org.altbeacon.beacon.BeaconIntentProcessor" android:enabled="true">
		</service>
		<!--
	    	Service running in the background that receives your Push Notifications 
	     -->
		<service android:name="re.notifica.push.gcm.PushService" android:label="Notificare Push Service" />
		<!--
	    	Receiver to dispatch GCM registration, push notifications and custom push actions.
	    	By default, it will register the device as an anonymous user, launch the default 
	    	Notification activity declared below when receiving notifications, and will 
	    	launch your main activity with a "custom action" intent when a user selected 
	    	a custom push action.
	    	 
	    	Most likely you will need customized registration and dispatching; please override 
	    	this class or the BaseIntentReceiver parent class.

	    	SecondActivity
	    	re.notifica.push.gcm.DefaultIntentReceiver
	     -->
		<!--
        	Notificare activity that will take care of displaying and handling notifications
        	The default activity will take care of everything, please implement your own
        	activity if you want to handle all user interaction yourself. 

        	re.notifica.ui.NotificationActivity
         -->
		<activity android:name="re.notifica.ui.NotificationActivity" />
		<activity android:name="re.notifica.ui.UserPreferencesActivity" />
	</application>
</manifest>